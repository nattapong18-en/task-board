# Local Compose defaults are suitable for practice. Change the password before
# using the deployment Compose file on another machine.
POSTGRES_DB=taskboard
POSTGRES_USER=taskboard
POSTGRES_PASSWORD=change-me

# Used by compose.deploy.yaml
DOCKERHUB_USERNAME=your-dockerhub-username
IMAGE_TAG=v1
WEB_PORT=8080

